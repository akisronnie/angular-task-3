import { HotelbyclickPipe } from './hotelbyclick.pipe';

describe('HotelbyclickPipe', () => {
  it('create an instance', () => {
    const pipe = new HotelbyclickPipe();
    expect(pipe).toBeTruthy();
  });
});
