import { ImglargeDirective } from './imglarge.directive';

describe('ImglargeDirective', () => {
  it('should create an instance', () => {
    const directive = new ImglargeDirective();
    expect(directive).toBeTruthy();
  });
});
